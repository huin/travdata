function expand_column_on_regex(cfg, rows)
    -- TODO
end

function fold_rows(cfg, rows)
    -- TODO
end

function join_columns(cfg, rows)
    -- TODO
end

function prepend_row(prepend, rows)
    -- TODO
end

function split_column(cfg, rows)
end

function transpose(rows)
    -- TODO
end

function wrap_row_every_n(cfg, rows)
    -- TODO
end
